# Netlify config for the Next.js (App Router) app. The Next.js Runtime deploys
# server components, API routes, and middleware automatically.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
